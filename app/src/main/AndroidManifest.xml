<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hugopc.rompecabezas2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash_screenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login2Activity"
            android:label="@string/title_activity_login2"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".AcercaDeActivity"
            android:label="@string/title_activity_acerca_de" />
        <activity android:name=".SeleccionActivity"
            android:label="@string/seleccionDif"

            />
        <activity android:name=".ListUsuariosActivity"
            android:label="@string/lista_usuarios"
            />
        <activity android:name=".PuzzleActivity"
            android:label="@string/rompe"
            />
        <activity android:name=".SelectImagenActivity"
            android:label="@string/seleccionaImagen"
            />
        <activity android:name=".Raking4Activity"
            android:label="@string/rankin4piezas"
            />
        <activity android:name=".Ranking8Activity"
            android:label="@string/ranking8piezas"
            />
        <activity android:name=".Ranking16Activity"
            android:label="@string/ranking16piezas"
            ></activity>
    </application>

</manifest>